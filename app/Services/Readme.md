## To add a new resource service eg (command)
1. Create a new service class in the `app/Services/Command` directory.
2. Implement the `CommonInterface` interface (if needed)
3. Extend the `BaseHttpClient` class (if needed)